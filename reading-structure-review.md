# 阅读结构专项复习

只收录反复造成理解障碍的结构模式；原句、译文和出处仍保留在 `sentence-review.md`。

## 001. 被插开的熟悉搭配

**问题：**认识搭配，但中间插入副词/短语后，无法重新连起来。  
**触发句：**`depending in part on ...`
```text
depending [in part] on ...
└────── depend on ──────┘
```
**结构：**核心 `depend on ...`（取决于）；插入 `in part`（部分地）；整体 = 部分取决于……。  
**阅读：**找两端 `depending`、`on` → 暂挂 `in part` → 还原 `depending on ...` → 补回程度信息。  
**同类：**`depend largely / entirely / partly on ...`。

## 002. 情态动词后的被动语态

**问题：**看到 `can be + 过去分词`，无法迅速识别谓语整体或动作接受者。  
**触发句：**`People can be shown round the building.`
```text
Staff can show people round the building.
执行者       动作   接受者
People can be shown round the building (by staff).
接受者       被动谓语                    执行者可省略
```
**结构：**`can + be + shown` = 情态动词 + 被动 `be` + `show` 的过去分词；`shown` 不是过去时。  
**阅读：**把 `can be shown` 当谓语块 → 看主语是否接受动作 → 必要时补出执行者 → 用主动句验证。  
**同类：**`Visitors can be guided through the museum.`；`The problem can be solved in several ways.`

## 003. 介词短语作表语

**问题：**看到 `be` 后接介词，误以为缺少表语，或只把介词理解为地点。  
**触发句：**`One way is by organising backstage tours.`
```text
One way | is | [by organising backstage tours].
主语       系动词   介词短语作表语
```
**结构：**`by`（方式介词）+ `organising backstage tours`（介词宾语）组成完整介词短语，作表语。  
**阅读：**`is by doing ...` → “办法/方式是通过做……”，不把 `by` 单独当表语。  
**对照：**`The key is in the drawer.`（地点）；`The meeting is at ten.`（时间）；`One solution is by reducing waste.`（方式）。
